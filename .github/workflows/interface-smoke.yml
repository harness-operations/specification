name: Harness interface smoke

on:
  pull_request:
    paths:
      - 'mappings/tests/**'
      - 'comparisons/data/landscape.json'
      - '.github/workflows/interface-smoke.yml'
  push:
    branches:
      - main
    paths:
      - 'mappings/tests/**'
      - 'comparisons/data/landscape.json'
      - '.github/workflows/interface-smoke.yml'

permissions:
  contents: read

jobs:
  exact-version-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Run exact-version interface smoke tests
        run: bash mappings/tests/interface-smoke.sh
